# Imports
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.animation as animation
from animation import generate_animation, example_matrices

# constantes
h, L = 50,50

generate_animation(example_matrices, interval=500)